# Required only for `sticker survey --live`. Never commit a real key.
CALLE_API_KEY=replace_me
CALLE_BASE_URL=https://api.heycall-e.com

# Second, independent gate. Live calling stays off until this is true.
STICKER_LIVE_CALLS_ENABLED=false
